// Config for accessing db

export const config: {
    DATABASE_NAME: string;
    USERNAME: string;
    PASSWORD: string;
} = {
    DATABASE_NAME: "pb2",
    USERNAME: "pb2_app",
    PASSWORD: "password",
};
